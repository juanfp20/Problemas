#El codigo resuelve el problema de encontrar el area de un triangulo
#Recibe como entrada dos numeros en referencia a la base y la altura. No se pasara a la siguiente accion si no se ingresa un numero correctamente
#Devuelve como salida el numero del area del triangulo.


while True:
	try:
		Base= int(input("ingrese la base del triangulo"))
		break
	except ValueError:
		print("Opps, ingrese nuevamente")
while True:
	try:
		Altura = int(input("ingrese la altura del triangulo"))
		break
	except ValueError:
		print("Opps, ingrese nuevamente")

Area = (Base*Altura)/2
print("El area es:")
print(Area)
